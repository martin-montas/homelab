---
- name: Docker prune and send alerts
  hosts: docker_vm
  become: true
  tasks:
    - name: prunes Docker 
      ansible.builtin.shell: |
        docker system prune -f

      register: command_status

    - name: Notify with Discord 
      ansible.builtin.uri:
        url: "{{ discord_webhook_url }}"
        method: POST
        headers:
          Content-Type: "application/json"
        body_format: json
        body:
          content: >
            🚨 Alert on the Docker machine {{ inventory_hostname }}:
            Docker Prune failed {{ command_status.stdout }}%
        status_code: 204
      when: command_status.rc != 0
